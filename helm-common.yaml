nameOverride: lti-launcher

namespace: ua-vpit--enterprise-systems--lms

image:
  repository: registry.docker.iu.edu/lms/lti-launcher
  tag: unstable
  pullPolicy: Always

replicaCount: 1

resources:
  requests:
    cpu: "0"
    memory: "0"
  limits:
    cpu: "1"
    memory: "1Gi"


imagePullSecrets:
  - name: registry-prod

service:
  type: ClusterIP
  port: 80